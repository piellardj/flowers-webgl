import "./page-interface-generated";

/* === IDs ============================================================ */
const controlId = {
};

/* === OBSERVERS ====================================================== */

/* === INTERFACE ====================================================== */
class Parameters {
    private constructor() {}
}

export {
    Parameters,
};
